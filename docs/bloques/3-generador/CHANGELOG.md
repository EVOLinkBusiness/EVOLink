# CHANGELOG — Bloque 3 (Generador web)

Registro de evolución del bloque. Cada corrección motivada por un fallo se anota en UNA entrada (la más reciente arriba) con este formato:

- **[YYYY-MM-DD]** [error detectado] → [qué se cambió y dónde] → aprobado por [socio]. Run #[id en `agent_runs`, o "manual" si no hay run].

Ejemplo ilustrativo (NO real, no copiar):
- **[2026-07-02]** Los formularios generados enviaban sin validar el email (el paso "evaluar" no lo cubría) → añadido check de validación de formulario a `rubrica.md` y al script de playwright-cli → aprobado por Kravitzz. Run #127.

Reglas: nunca borrar entradas · si un cambio se revierte, se añade entrada nueva explicándolo · el bloque 7 (Mejora) usará este historial para validar que las correcciones siguen vigentes.

---

- **[2026-06-26] · Auditoría de diseño v4 (12 previews) + fallo arquitectónico de slot fijo.** Auditadas las 12 previews vigentes de mudanzasroy con criterio de diseñador → `referencias/auditoria-diseno-v4.md`. Veredicto: 6 conservar (listón "elegante y fluido" = `fab-3` word-scrub, `gsap-8` Flip+DrawSVG, `gsap-10` scrub contundente), 5 arreglar (desajuste de contenido en `fab-2`/`fab-4` que asumen fotos inexistentes; motion impertinente en `gsap-9`/`anim-5`; 3D decorativo en `gsap-11`), 1 **descartar como concepto** = `gsap-12` (cubo 3D protagonista). **Fallo de raíz documentado:** el reparto por slot fijo (1-3 propias / 4-6 lapa / 7-8 frontend / 9-10 tasteskill / **11-12 Three.js**) *obligaba* a un motor por posición → el slot 11-12 forzó el 3D y, sin uso pertinente en una mudanza, se rellenó con un cubo decorativo. No fue fallo de ejecución sino de arquitectura: la regla de "pertinencia" no estaba operacionalizada. Corrección de raíz = `director-arte` (v4). Aprobado por Kravitzz. Run: manual.
- **[2026-06-16]** preview-3 (kinético bold, ex-preview-4) heredó el patrón OLD `.js .reveal` sin `setTimeout` de seguridad: riesgo de pantalla en blanco permanente si el IO no disparaba + bug de especificidad `prefers-reduced-motion` (`.js .hero h1 .word span` 0,3,2 gana a `@media` 0,2,2) → actualizado a patrón v2 robusto: eliminado `.js .reveal{opacity:0}` y `.js .hero h1 .word span`; título hero a CSS `@keyframes word-rise` (independiente de IO/JS); `.reveal.is-hidden` como estado oculto; IO+`getBoundingClientRect()` pre-check (elementos ya visibles nunca se ocultan)+`setTimeout(revealAll,1200)` de seguridad. Detector `[]` · 0 mojibake. Aprobado por Kravitzz. Run: manual.
- **[2026-06-16] · Reparto de previews ampliado a 9 (3+3+3), nueva despensa de estilos lapa.ninja (decisión de socio, no fue un fallo)** — Tras la ronda de 8 previews (5 despensa + 3 lapa.ninja de prueba), Kravitzz valoró el trabajo como correcto pero, por gusto subjetivo, descartó las previews 3, 7 y 8 y pidió ampliar el reparto. Cambios: (1) `flujo-previews.md` pasa de 8=5+1+2 a **9=3+3+3** (propias / lapa.ninja / Stitch); (2) nueva referencia `estilos-lapa-ninja.md` — pool de géneros de layout/paleta (Corporate, Bento Grid, Magazine/Editorial, Minimal/Studio, Retro/Pattern, Real Estate/Furniture, SaaS suave) mapeados a perfiles EVOLink, para diversificar Etapa 3b sin clonar lapa.ninja; (3) `stitch-prompts.md` ampliado a 3 voces (Stitch-7/8/9); (4) investigada automatización Stitch vía MCP/API → documentada en `stitch-integracion.md`: requiere `gcloud` CLI + `gcloud auth application-default login` (NO `STITCH_API_KEY`), pendiente de instalación/autenticación del socio. Reorganización de archivos: preview-4 (kinético/framer) pasa a slot 3; antiguas preview-3 (vercel, descartada) y preview-5 (superlist, descartada por elección del socio) eliminadas; nuevas preview-4/5/6 generadas con géneros Magazine/Editorial (Spectral+Hanken Grotesk), Real Estate/Furniture (Schibsted Grotesk+Onest) y Retro/Pattern (Archivo+Hanken Grotesk, paleta crema/mostaza/oliva — distinta de la retro-con-bordes ya descartada); preview-7/8/9 son placeholders "pendiente" hasta generación manual en Stitch. Las 9 pasan el detector con `[]`. Aprobado por Kravitzz. Run: manual.
- **[2026-06-16]** preview-3 (despensa v2, arquetipo vercel) se renderizaba en blanco bajo todas las secciones (solo header y bordes de grid visibles): el patrón `.js .reveal{opacity:0;...}` + `.reveal.in{opacity:1;...}` + `IntersectionObserver` dependía de que el `.in` llegara a añadirse, y la regla de `prefers-reduced-motion` (specificity 0,1,0) no podía ganarle a `.js .reveal` (0,2,0) si el IO no disparaba → cambiado a patrón "oculto solo por JS": CSS por defecto deja `.reveal` visible (sin regla `.js .reveal{opacity:0}`); el script añade `.is-hidden` (oculta+clip-path) únicamente si hay JS, no hay `prefers-reduced-motion: reduce`, e IntersectionObserver existe; con try/catch + `setTimeout` de 2,5s como red de seguridad que revela todo si algo falla. Aplicado solo a preview-3 (los otros 4 ya OK por revisión del socio). Recomendado adoptar este patrón en futuras previews del despensa para evitar la recurrencia → aprobado por Kravitzz. Run: manual (recuperación previews mudanzasroy).
- **[2026-06-14] · Piloto real mudanzasroy en local: cadena Auditor→Generador verificada.** Del `result` del Auditor + datos del cliente → brief, marca, plan, build Astro y evaluación **Lighthouse móvil 100** (0 rotos, AA, formulario OK); 5 etapas registradas en `agent_runs`. Probado el sistema de previews: 5 direcciones (`ui-ux-pro-max`), 5 motions (`ui-animation`) y 5 GSAP "fabulosas" (`gpt-tasteskill`), con logo + imágenes reales de la web del cliente. Elección final del socio: **F5 (Editorial Bold + kinético)**, registrada en `agent_runs` (`preview-choice`). **Veredicto del socio:** la cadena (Auditor→Supabase→Generador) funciona; la **estética del catálogo se ve "simple"** y se mejorará en sesiones aparte (traer el lenguaje ganador + 5-previews + motion al motor). Datos de prueba: conservados los 11 runs, borrados clientes/audits duplicados. Run: manual (cliente `cb1dfbea`).
- **[2026-06-14] · REGLA FIJA del pipeline (decisión de socio, no fue un fallo)** — Durante el piloto mudanzasroy, la salida directa del catálogo se vio "simple/poco vendible". Decisión de Kravitzz, permanente: el Generador **siempre prepara 5 previews**, cada una con **diseño Y mecánicas de animación distintas** (Etapa 3b; skills `ui-ux-pro-max` estilo/paleta/tipo, `ui-animation` micro-motion, `gpt-tasteskill` macro-motion/GSAP, `frontend-design`); los socios **eligen 1 en el Checkpoint 1** y **se borran las otras 4**; de la ganadora se elabora la web final (Etapas 4-5, catálogo determinista). **Mix-and-match permitido** (diseño de la N + motion de la M). **Aprendizaje estadístico:** la elección se registra en `agent_runs` (stage `preview-choice`, `output:{design,motion,mixed}`) y el bloque 7 mina la frecuencia para priorizar las direcciones más elegidas. Añadido a `generador-web/SKILL.md` (Etapa 3b + Checkpoint 1). Aprobado por Kravitzz. Run: manual (piloto mudanzasroy, cliente `cb1dfbea`).
- **[2026-06-13] · DIAGNÓSTICO v1 (cabecera de sesión)** — Auditoría profunda previa al piloto. **Robustez (FASE 1):** suite adversaria de entrada válida-pero-hostil (HTML/`<script>`, comillas, emojis, ñ/tildes, opcionales ausentes, textos ~1800 chars) + build real. Resultado: 0 fugas `null`/`undefined`/`[object Object]`, escapado completo de Astro verificado (atributos y RCDATA; `og:title` con `<script>` es inerte porque la `"` se escapa a `&#34;` → sin XSS). **1 bug** (ver entrada siguiente). **Skills (FASE 2):** sin contradicciones duras entre `frontend-design` (método, 1º) / `estilo-evolink` (voz de proyecto, 2º, precedencia) / `generador-web` (orquesta); enforcement de fuente única en `rubrica.md`+`evaluate`; añadido el orden explícito al orquestador (commit `d3b1c46`). **Calidad (FASE 3):** 3 webs ficticias (reformas-garcia/novabano/montanes) con paletas, variantes y mezclas de sección distintas → variedad real, no plantilla, copy específico; las 3 **PASAN** (Lighthouse móvil 91/94/98, 0 rotos, AA, formulario OK). 2 hallazgos de calidad que el gate ≥90 caza solo: (a) `google_fonts` vía `<link>` hunde el móvil bajo 90 (86→91 al quitarlo; v1 sin self-hosting → usar stack de sistema, ya mandado por `estilo-evolink §2`); (b) HeroC a sangre con imagen remota sobredimensionada penaliza LCP (1600×900→86; 1024×576→94 → limitar dimensiones del hero). Aprobado por Kravitzz. Runs: manual (3 muestras).
- **[2026-06-13]** `validatePlanPagina` exigía `nombre_negocio` en el Footer aunque `injectDefaults` lo deriva de `marca` (como en Header): un `plan-pagina.json` del LLM que lo omitiera fallaba en vez de degradar → quitado de `Footer.required` en `schema.ts` + test de regresión en la suite adversaria → aprobado por Kravitzz. Run: manual (diagnóstico FASE 1, commit `67b6abb`).
- **[2026-06-13]** En FASE 8, re-ensamblar borra `site/` entero (incl. `node_modules`) y el build de `evaluate` fallaba (`astro` no encontrado) → `evaluate.cli.ts` hace `npm install` automático si falta `node_modules` antes de construir (pipeline reanudable tras un ensamblado limpio) → aprobado por Kravitzz. Run: manual (E2E `demo`).
- **[2026-06-13]** El checker de enlaces (`evaluate-checks.ts`) marcaba como roto el `href` de `<link rel=stylesheet>` (`/_astro/*.css`), no solo las anclas → `findBrokenLinks` ahora solo escanea `<a href>` (los assets los gestiona el build) → aprobado por Kravitzz. Run: manual (E2E `demo`, FASE 8).
- **[2026-06-13]** Lighthouse fallaba con EPERM al crear su `user-data-dir` en el temp global del sistema (entorno restringido) → `evaluate.cli.ts` lanza chrome-launcher con un `userDataDir` local dentro de `clientes/<id>/eval/` → aprobado por Kravitzz. Run: manual (E2E `demo`, Lighthouse 98).
