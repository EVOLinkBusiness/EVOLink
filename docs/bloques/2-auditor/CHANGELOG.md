# CHANGELOG — Bloque 2 (Auditor)

Registro de evolución del bloque. Cada corrección motivada por un fallo se anota en UNA entrada (la más reciente arriba) con este formato:

- **[YYYY-MM-DD]** [error detectado] → [qué se cambió y dónde] → aprobado por [socio]. Run #[id en `agent_runs`, o "manual" si no hay run].

Ejemplo ilustrativo (NO real, no copiar):
- **[2026-07-02]** El scoring daba 0 a negocios con ficha de Google Maps válida (falso negativo en el motor de reglas) → añadida regla "ficha de Maps verificada ⇒ score base ≥ 20" a la rúbrica de scoring → aprobado por Kravitzz. Run #127.

Reglas: nunca borrar entradas · si un cambio se revierte, se añade entrada nueva explicándolo · el bloque 7 (Mejora) usará este historial para validar que las correcciones siguen vigentes.

---
- **[2026-06-14]** Piloto real mudanzasroy (cliente CON web propia) destapó un bug factual: la narrativa afirmaba *"no existe página web"* / *"ausencia total de presencia digital"* y recomendaba "lanzar una web" que ya existe. Raíz: `generate-audit` no cargaba `clients.website_url` ni lo pasaba a la narrativa, y el `SYSTEM` de `_shared/llm.ts` asumía "negocios SIN página web" → la narrativa alucinaba la ausencia → cargado `website_url` en `ClientRow`/select, añadido `websiteUrl` a `NarrativeInput` + línea "Página web propia" en el prompt, y regla en el SYSTEM (web existente = activo; nunca negarla ni recomendar crearla) en `_shared/llm.ts` + `generate-audit/index.ts`. Banco de regresión: 4 tests nuevos (`llm.test.ts` ×3, `handler.test.ts` ×1), suite **35→39 en verde**. Verificado con el modelo real: run `8fe639d0` (bug, decía "no web") → tras el fix run `fa6d78c5` abre con *"cuenta con un activo digital sólido: una web propia (mudanzasroy.es)"* y recomienda *optimizar* la web existente; 0 frases de "no web". **Desplegado: `generate-audit` v6 ACTIVE.** `overall=null` se mantiene (correcto: sin datos de Maps) → aprobado por Kravitzz. Runs `8fe639d0`→`fa6d78c5`.
- **[2026-06-13]** Smoke test real del camino de visión de `extract-presence` (pendiente desde ETAPA 4), con captura real de una ficha de Maps ("MG Reformas Integrales"). Resultado **correcto, sin bugs**: extrajo nombre, categoría, teléfono, dirección, horario, 116 reseñas y nota 5,0; `website_url: null` (no tiene web); `has_maps_listing: true`; `missing_required: [niche, service_area, has_website]` (los rellena el humano); `formToClient` pobló `gbp` + `reviews` correctamente. Coste real **$0,0027** (Haiku, 2135 in / 120 out). Path de visión validado end-to-end → aprobado por Kravitzz. Run manual (script de piloto, descartado tras correr).
- **[2026-06-13]** Revisión profunda + caza de bugs del bloque (previa a ETAPA 2). Hallazgo confirmado: `generate-audit` sumaba los tokens de la narrativa (Opus 4.8, $5/$25) y los del supervisor (Haiku 4.5, $1/$5) en un único contador y aplicaba la tarifa Opus a todo → `agent_runs.cost` quedaba **sobreestimado** (el dato que minará el bloque 7) → tarifado por modelo con contadores separados en `supabase/functions/generate-audit/index.ts` + test de regresión (`handler.test.ts`). Verificado: el test reproducía el bug (coste 0,0195 vs 0,0179 correcto) y pasa tras el fix → aprobado por Kravitzz. Run manual (commit `632424e`; suite 35/35). **Desplegado a producción: `generate-audit` v5 ACTIVE (2026-06-13).**
- **[2026-06-13]** Endurecimiento (no era bug vivo): `extractFromScreenshot` forzaba `has_maps_listing: true` a ciegas. Hoy el `EXTRACT_SCHEMA` (`additionalProperties:false`) impide que la visión emita ese campo, así que el default siempre gana; pero la sobrescritura descartaría la señal en silencio si el schema gana en el futuro un indicador "esto no es ficha de Maps" → default a `true` solo si ausente (`raw.has_maps_listing ?? true`) en `_shared/intake/extract.ts` + test de contrato → aprobado por Kravitzz. Run manual (commit `130b133`).
- **[2026-06-13]** Limitaciones documentadas en la misma revisión (NO se tocan, por diseño/diferido): (a) ambas Edge Functions devuelven JSON sin cabeceras CORS → bloqueará el dashboard Plan B (Next, `/r/[slug]`) cuando se construya. (b) En `extract-presence`, un run fallido tras consumir tokens registra `cost: 0` (la `usage` no se propaga en la excepción) → sub-reporta coste solo en fallos (raros y baratos). (c) `last_review_months_ago` no tiene campo en `IntakeForm` → la recencia de reseñas suma siempre 0 desde el intake manual. Revisar al construir el Plan B / la automatización.
- **[2026-06-13]** El primer smoke test real destapó dos fallos: (1) el `default` de `audits.share_slug` usaba `encode(..., 'base64url')`, codificación inexistente en Postgres → cada `INSERT` reventaba (error 22023), ninguna auditoría podía guardarse → migración `20260613000001_fix_share_slug_encoding.sql` (base64 traducido a URL-safe) + corrección del esquema original. (2) El modelo devolvía `dimension` como etiqueta legible ("Google Business Profile") en vez de la clave → `ruleChecks` no casaba la cobertura y generaba 7 flags espurios por auditoría → `enum` de las 7 claves en `NARRATIVE_SCHEMA` + instrucción en el prompt (`_shared/llm.ts`). Verificado: run `e55cdfe5` con `supervisor_flags: []`, 7 hallazgos por clave, coste $0,062 → aprobado por Kravitzz. Run `dffb63b6` (fallo) → `e55cdfe5` (ok).
- **[2026-06-13]** La spec original dejaba la entrada de datos sin estructura ("manuales en v1"), inconsistente y no preparada para la automatización → puerta de entrada formal (anexo `docs/superpowers/specs/2026-06-12-auditor-v1-anexo-entrada.md`): contrato `IntakeForm` + `validateForm`/`formToClient` en `supabase/functions/_shared/intake/form.ts`, extracción por visión (Haiku 4.5) desde captura de Maps en `_shared/intake/extract.ts`, y Edge Function `extract-presence` que devuelve borrador para confirmación humana. El scoring sigue intacto: la captura nunca lo alimenta, solo rellena el formulario confirmado → aprobado por Kravitzz (decisión de diseño pre-código). Run manual (sin run; 16 tests nuevos en verde). que `llmCheck` (supervisor) lanzaba excepción si la salida de Haiku no traía el campo `flags`, tumbando el run entero → parseo defensivo en `supabase/functions/_shared/supervisor.ts` (flags ausentes/malformados ⇒ `[]`, el run continúa) → aprobado por Kravitzz (ejecución del plan aprobado). Run manual (test, commit `b386b92`).
- **[2026-06-11]** Los advisors de Supabase avisaron de `search_path` mutable en `set_updated_at` y de `is_admin()` ejecutable por `anon`; además el proyecto tiene default privileges recortados (sin SELECT/INSERT ni para `service_role`, habría roto la Edge Function) → migraciones `20260611000003_hardening.sql` + `20260611000004_grants.sql` → aprobado por Kravitzz. Run manual (commit `f598585`).
